---
description: 剧说 Flutter 多语言与文案规则
globs: "lib/**/*.dart,lib/l10n/**"
alwaysApply: false
---

# 05-多语言支持

## 1. 当前策略

当前默认中文，但工程保留 `l10n/` 和 `generated/` 目录。复杂页面不得把大量用户可见文案散落在 Widget 中。

## 2. 文案分层

- 高频通用文案：集中到常量或后续 l10n。
- 页面私有短文案：可临时放页面模块，但需要清晰可迁移。
- 价格、积分、时长、日期：统一走格式化工具。
- 协议、规则、隐私政策：走 WebView 或配置，不硬编码长文本。

## 3. 格式化

- 积分：`AppFormatters.points`
- 时长：`AppFormatters.duration`
- 日期：后续统一在 `AppFormatters`
- 价格：后续按支付策略确认

## 4. 禁止事项

- 不在多个页面复制同一段长文案。
- 不把运营规则奖励金额写死在 UI。
- 不把支付价格和周期散落在 Widget。
