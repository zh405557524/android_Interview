---
description: Bug修复任务规则
globs: "lib/**/*.dart,pubspec.yaml,test/**/*.dart,.cursor/rules/**/*.mdc"
alwaysApply: false
---

# Bug修复任务规则

## 修复流程

### 1. 问题定位
- **必须**先阅读相关代码，理解问题发生的上下文
- 查看错误日志、异常信息（如果有）
- 检查相关的测试用例或用户反馈
- 使用 `grep` 或 `codebase_search` 查找相关代码

### 2. 根本原因分析
- 不要只修复表面症状，要找到根本原因
- 分析为什么会出现这个问题
- 考虑是否有其他类似的问题

### 3. 修复原则
- **最小化修改**：只修改必要的代码，避免引入新的问题
- **保持一致性**：修复方式要与项目现有代码风格一致
- **向后兼容**：确保修复不会破坏现有功能
- **添加防护**：在关键位置添加必要的错误检查和边界条件处理

### 4. 错误处理
- 添加适当的 try-catch 块
- 记录错误日志（使用 logger）
- 向用户显示友好的错误提示
- 避免静默失败

### 5. 测试验证
- 验证修复是否解决了问题
- 检查是否引入了新的问题
- 考虑边界情况和异常情况

## 代码规范

### GetX Controller
- 确保正确使用 `update()` 通知UI更新
- 在 `onClose()` 中正确释放资源
- 处理异步操作的错误情况

### API调用
- 添加网络错误处理
- 处理超时情况
- 验证返回数据的有效性

### 状态管理
- 确保状态更新的原子性
- 避免竞态条件
- 正确处理异步状态更新

## 检查清单

修复完成后，检查以下项目：

- [ ] 问题已完全解决
- [ ] 没有引入新的bug
- [ ] 代码符合项目规范
- [ ] 错误处理已添加
- [ ] 资源已正确释放（如需要）
- [ ] 日志记录已添加（如需要）
- [ ] 用户提示已添加（如需要）

## 示例修复模式

### 空指针检查
```dart
// ❌ 错误
String name = user.name;

// ✅ 正确
String name = user?.name ?? '';
```

### 异步错误处理
```dart
// ❌ 错误
final result = await api.getData();

// ✅ 正确
try {
  final result = await api.getData();
  // 处理结果
} catch (e) {
  logger.e('获取数据失败', error: e);
  CustomToast.text('操作失败，请稍后重试');
}
```

### Controller资源释放
```dart
@override
void onClose() {
  textController.dispose(); // ✅ 确保释放资源
  super.onClose();
}
```
