---
description: 剧说 Flutter 规则目录索引与规则新增约束
globs: "lib/**/*.dart,pubspec.yaml,test/**/*.dart,.cursor/rules/**/*.mdc"
alwaysApply: true
---

# 00-规则目录索引

## 1. 常驻规则

对话开启后默认读取以下常驻规则：

| 文件 | 用途 |
|------|------|
| `00-规则目录索引.mdc` | 规则目录、动态加载说明、新增规则约束 |
| `02-项目结构目录.mdc` | Flutter 工程目录、页面模块结构、数据层与服务层级 |
| `03-编码规范.mdc` | 命名、模块组织、状态管理、Controller/UI 边界、成员顺序、注释与验证 |
| `10-编码工作流程.mdc` | 每轮任务判断、动态加载规则、实现顺序、验证和暂停条件 |

除以上文件外，其他规则默认不是常驻规则，必须由 `10-编码工作流程.mdc` 按任务类型动态加载。

## 2. 动态项目规则

| 文件 | 触发场景 |
|------|----------|
| `01-项目初始化规则.mdc` | 基础工程结构缺失、初始化缺口、入口无法识别 |
| `04-导航规则.mdc` | 路由注册、跳转、参数、登录守卫、回传结果 |
| `05-多语言支持.mdc` | 用户可见文案、格式化、协议 / 隐私 / 规则文案 |
| `06-第三方依赖参考.mdc` | 新增、删除、升级依赖或接入 SDK |
| `09-大功能先规划再编码.mdc` | 多页面、多模块、跨主链路的大功能推进 |

## 3. 任务专项规则

| 文件 | 触发场景 |
|------|----------|
| `task-new_feature.mdc` | 新功能、新页面、新模块、新弹层、新 Controller |
| `task-ui_task.mdc` | UI 编码、Figma 对稿、图片资源、弹框、样式修复 |
| `task-bug_fix.mdc` | 崩溃、报错、行为异常、测试失败 |
| `task-logic_change.mdc` | 业务规则、状态流转、接口调用顺序调整 |
| `task-refactor.mdc` | 不改变行为的代码结构整理 |
| `task-project_init.mdc` | 仅在确认存在项目初始化缺口时作为初始化模板参考 |
| `task-other.mdc` | 无法明确归类的 Flutter 代码任务 |

## 4. 新增规则约束

- 新增规则文件前，必须先查本索引和现有规则。
- 能合并到现有规则的，不新建文件。
- 新规则必须有明确触发场景、不能与已有规则职责重叠。
- 新规则创建后必须同步更新本索引和 `10-编码工作流程.mdc` 的动态加载说明。
- 若规则之间冲突，以更具体的任务专项规则为准；任务规则未覆盖时，以常驻规则和动态项目规则为准。
