---
description: 剧说 Flutter 编码工作流程
globs: "lib/**/*.dart,pubspec.yaml,test/**/*.dart"
alwaysApply: true
---

# 10-编码工作流程

## 1. 对话开启规则

对话开启后默认读取常驻规则：

1. `00-规则目录索引.mdc`
2. `02-项目结构目录.mdc`
3. `03-编码规范.mdc`
4. `10-编码工作流程.mdc`

本文件负责每轮任务判断和动态加载规则。除常驻规则外，不提前加载所有规则；根据任务类型和涉及范围按需读取。

## 2. 编码阶段原则

Flutter 编码阶段是连续执行阶段。用户要求开始代码编写后，按模块文档持续推进，不在每个模块后等待确认。开发完成后由用户统一验收。

本工程当前已完成初始化，后续默认进入「功能开发循环」。只有发现项目 `.cursor/rules/` 缺失、基础工程结构缺失或 `pubspec.yaml` / 入口文件无法识别时，才重新进入初始化分支。

规则内容本身不按当前代码让步；若项目代码与规则不一致，视为后续代码整改问题。

## 3. 每轮输入先判断

1. 是否是规则输出 / 架构确认任务。
2. 是否存在初始化缺口：
   - `.cursor/rules/` 项目规则缺失。
   - `pubspec.yaml` 缺失或不可识别。
   - `lib/main.dart`、`lib/global.dart`、`lib/routes/`、`lib/services/`、`lib/store/`、`lib/utils/`、`lib/widgets/`、`lib/pages/` 任一基础结构缺失。
3. 若没有初始化缺口，进入功能开发循环。

规则输出任务完成后需要暂停等待用户确认。

## 4. 动态加载规则

功能开发循环内，每轮再判断任务类型并动态加载规则：

| 任务类型 | 动态加载规则 | 使用场景 |
|----------|--------------|----------|
| 新功能开发 | `task-new_feature.mdc` | 默认类型；新建页面、模块、弹层、路由、Controller |
| UI 编码 / 对稿 | `task-ui_task.mdc` | 视觉、图标、布局、状态 UI、Figma 对稿 |
| Bug 修复 | `task-bug_fix.mdc` | 崩溃、报错、行为异常、测试失败 |
| 业务逻辑修改 | `task-logic_change.mdc` | 业务规则、状态流转、接口调用顺序调整 |
| 重构 | `task-refactor.mdc` | 不改变行为的结构整理 |
| 其他 | `task-other.mdc` | 无法明确归类的 Flutter 代码任务 |
| 初始化缺口 | `01-项目初始化规则.mdc`、`task-project_init.mdc` | 基础结构缺失、入口不可识别 |

若任务涉及以下内容，再追加加载对应项目规则：

| 涉及内容 | 追加加载规则 |
|----------|--------------|
| 路由注册、跳转、参数、回传 | `04-导航规则.mdc` |
| 多语言、用户可见文案、格式化 | `05-多语言支持.mdc` |
| 新增、删除、升级依赖或 SDK | `06-第三方依赖参考.mdc` |
| 多页面、多模块、跨主链路大功能 | `09-大功能先规划再编码.mdc` |

多轮交互时，按用户最新反馈重新判断任务类型；若用户只说「继续」，默认继续当前功能开发任务。

## 5. UI 编码前置

UI 编码前必须确认 Figma 可通信。无法通信时暂停并询问用户。

UI 任务必须读取 `task-ui_task.mdc`，并遵守 Figma、图片资源、AppBar、CustomScaffold、页面结构、图标适配等项目 UI 规则。

## 6. 实现顺序

```text
模型 / 参数
  -> 真实 API 静态方法
  -> Store / Service
  -> Controller
  -> Controller 私有 mock / real 分支
  -> View / Widgets / Dialog
  -> 状态补齐
  -> analyze / test
  -> 记录结果
```

## 7. 验证

每轮至少执行：

```bash
flutter analyze
```

如测试存在或改动影响主链路，执行：

```bash
flutter test
```

完成后运行：

```bash
git diff --check
```

仅修改规则文档时，不需要 `dart format`、`flutter analyze` 或 `flutter test`。

## 8. 记录

完成编码后，需要记录本轮完成的模块、验证命令和遗留风险。流程侧记录由流程工作区负责，不写入本工程规则。

## 9. 暂停条件

只在以下情况暂停：

- 用户明确要求暂停或确认。
- 规则输出完成，需要用户检查规则。
- Figma 未连接且当前任务需要 UI。
- 验证失败且无法自行修复。
- 需求、接口或供应商缺失导致 mock 也无法合理表达。
