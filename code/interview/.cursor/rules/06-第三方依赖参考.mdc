---
description: 剧说 Flutter 第三方依赖规则
globs: "pubspec.yaml"
alwaysApply: false
---

# 06-第三方依赖参考

## 1. 已确定依赖

| 依赖 | 用途 |
|------|------|
| `get` | GetX 状态管理 |
| `go_router` | 路由 |
| `dio` | 网络 |
| `get_storage` | 轻量存储 |
| `hive` / `hive_flutter` | 结构化本地缓存 |
| `package_info_plus` | App 信息 |
| `flutter_screenutil` | 屏幕适配 |
| `flutter_localizations` | Flutter 本地化 |
| `intl` | 格式化 |

## 2. 暂不引入

供应商或策略未确认前，不引入：

- 运营商一键登录 SDK
- 短信 SDK
- 微信 / 支付宝 / IAP 支付 SDK
- 视频上传 SDK
- 视频播放 SDK
- APM / 崩溃统计 SDK

## 3. 引入新依赖前检查

1. 依赖是否服务当前模块。
2. 是否已有 Flutter / Dart 标准能力或现有依赖可完成。
3. 是否涉及隐私、支付、上传、登录等合规风险。
4. 是否需要同步隐私政策或商店合规清单。

## 4. 依赖更新验收

- `flutter pub get`
- `flutter analyze`
- 必要时 `flutter test`
